---
'@runtypelabs/mixlayer-ai-provider': major
---

Update the provider for AI SDK v7: depend on `@ai-sdk/openai-compatible` v3, require `ai` v7 as the peer, raise the Node.js requirement to 22+, and publish ESM-only artifacts.
